# План реализации второй части Norravia

## Результат

Один статический сайт GitHub Pages с двумя переключаемыми дизайн-версиями (`Arctic Day`, `Polar Night`), полным RU/EN, ночной покадровой сценой, новыми изображениями, SEO-улучшениями, отчётом `AI_AUDIT.md` и проверенной публикацией.

## 1. Подготовка исходной точки

- Работать поверх удалённой ветки `main` через локальную ветку `codex/homework-2`.
- Не изменять структуру и тексты, утверждённые в первой части, кроме локализации, исправлений доступности и точечного polish.
- Сохранить карту, таймер, меню, сопровождающий самолётик и работу через `file://`.

## 2. Новые изображения

- Создать базовый ночной первый кадр из утверждённой дневной композиции.
- Создать ещё четыре согласованных кадра с неподвижным самолётом и изменяющимися облаками/светом.
- Создать ночное крыло над фьордами и северным сиянием.
- Создать ночной салон Norravia без людей.
- Проверить изображения визуально.
- Сохранить качественные исходники и WebP; подготовить уменьшенные WebP для мобильных экранов.

## 3. Дизайн-токены и переключатель

- Добавить системные токены для Day и Night.
- Добавить доступные группы кнопок `DAY / NIGHT` и `RU / EN` в шапку.
- Встроить мобильное размещение переключателей.
- Реализовать сохранение выбора с безопасным fallback.
- Добавить theme transition и reduced-motion вариант.

## 4. Polar Night

- Настроить тёмную поверхность всех секций, навигации, кнопок и карточек.
- Настроить ночную карту и состояния интерактивных точек.
- Подключить отдельные ночные фотографии и разные маски/кадрирование.
- Реализовать последовательность пяти кадров первого экрана с отложенной загрузкой и отказоустойчивостью.

## 5. RU/EN

- Создать словари всех пользовательских текстов и доступных названий.
- Разметить переводимые элементы стабильными ключами.
- Локализовать карту, данные стран, таймер, меню, кнопки, подвал, aria-label, title и description.
- Сохранить активный маршрут при смене языка.

## 6. Visual polish и доступность

- Исправить активные display letter-spacing ниже `-0.04em`.
- Удалить лишний `tabindex` у неинтерактивных преимуществ.
- Увеличить зоны касания точек карты и кнопок стран до 44 пикселей.
- Исправить инструкцию повторного выбора карты.
- Унифицировать состояния hover/focus/active и проверить контраст.
- Сохранить все существующие reduced-motion альтернативы.

## 7. Производительность и очистка

- Добавить `srcset` и `sizes` для крупных фотографий.
- Сохранить preload только для дневного LCP-кадра.
- Удалить неиспользуемые HTML-шаблоны, стили и JavaScript старых билетов.
- Проверить внутренние ссылки, ID, alt, размеры и отсутствие отсутствующих ресурсов.

## 8. SEO

- Добавить canonical, robots meta, Open Graph, Twitter Card и JSON-LD WebSite.
- Добавить favicon на основе существующего знака.
- Создать `robots.txt`, `sitemap.xml` и `.nojekyll`.
- Использовать существующий GitHub Pages URL как единственный canonical.

## 9. Документация

- Создать `AI_AUDIT.md` с исходной оценкой, проблемами, решениями и результатом.
- Обновить `README.md`, `PROJECT_STATUS.md`, `AGENTS.md` и описание изображений.

## 10. Проверка и публикация

- Выполнить статические проверки HTML, CSS, JavaScript и SEO-файлов.
- Запустить локальный сервер и проверить Day/Night × RU/EN на desktop/mobile.
- Проверить карту, таймер, самолётик, клавиатуру, reduced motion, консоль и переполнение.
- Зафиксировать итоговый коммит в `codex/homework-2`.
- Отправить ветку в `kseniya01091983-oss/Norravia` и опубликовать итог в `main` согласованным способом.
- Проверить GitHub Pages и ключевые ресурсы после обновления.
